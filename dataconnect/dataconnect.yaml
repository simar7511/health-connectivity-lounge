specVersion: "v1beta"
serviceId: "health-connectivity-lounge"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "health-connectivity-lounge-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
